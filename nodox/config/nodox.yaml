nodox:
  version: "0.2"

paths:
  scan_path: "."
  canary_path: "."
  encrypt_path: "."
  decrypt_path: "."

scanner:
  max_file_size_mb: 2
  ignored_extensions:
    - ".png"
    - ".jpg"
    - ".jpeg"
    - ".gif"
    - ".pdf"
    - ".zip"
    - ".rar"
    - ".exe"
    - ".bin"
    - ".iso"
    - ".nodox"

encryptor:
  encrypted_extension: ".nodox"
  use_env_key: true
  env_key_name: "NODOX_KEY"
  # Crear backup antes de cifrar (recomendado)
  create_backup: true
  backup_dir: ".nodox_backups"
  # Usar derivación de clave PBKDF2 (más seguro pero más lento)
  # Si es true, NODOX_KEY puede ser una contraseña simple
  use_key_derivation: false

canary:
  directory: ".nodox_canary"
  check_interval_seconds: 5
  files:
    - "clientes_2025_CONFIDENCIAL.xlsx"
    - "passwords_admin.txt"
    - "contratos_privados.docx"
    - "backup_db.sql"

exfiltration:
  check_interval_seconds: 5
  threshold_mb: 50

logging:
  enabled: true
  level: "INFO"
  file: "logs/nodox.log"
